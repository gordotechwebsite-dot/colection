[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "RUF"]
# B008: Depends()/Query() en valores por defecto es el patrón normal de FastAPI.
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
